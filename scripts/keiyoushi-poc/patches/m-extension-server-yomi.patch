diff --git a/server/src/main/kotlin/eu/kanade/tachiyomi/network/NetworkHelper.kt b/server/src/main/kotlin/eu/kanade/tachiyomi/network/NetworkHelper.kt
index f7a664c..e404f46 100644
--- a/server/src/main/kotlin/eu/kanade/tachiyomi/network/NetworkHelper.kt
+++ b/server/src/main/kotlin/eu/kanade/tachiyomi/network/NetworkHelper.kt
@@ -10,6 +10,7 @@ package eu.kanade.tachiyomi.network
 
 import android.content.Context
 import eu.kanade.tachiyomi.network.interceptor.CloudflareInterceptor
+import eu.kanade.tachiyomi.network.interceptor.NoZstdInterceptor
 import eu.kanade.tachiyomi.network.interceptor.UncaughtExceptionInterceptor
 import eu.kanade.tachiyomi.network.interceptor.UserAgentInterceptor
 import okhttp3.Cache
@@ -30,6 +31,7 @@ class NetworkHelper(
                 .addInterceptor(UncaughtExceptionInterceptor())
                 .addInterceptor(UserAgentInterceptor(::defaultUserAgentProvider))
                 .addInterceptor(CloudflareInterceptor())
+                .addNetworkInterceptor(NoZstdInterceptor())
                 .connectTimeout(30, TimeUnit.SECONDS)
                 .readTimeout(30, TimeUnit.SECONDS)
                 .callTimeout(2, TimeUnit.MINUTES)
diff --git a/server/src/main/kotlin/eu/kanade/tachiyomi/network/interceptor/NoZstdInterceptor.kt b/server/src/main/kotlin/eu/kanade/tachiyomi/network/interceptor/NoZstdInterceptor.kt
new file mode 100644
index 0000000..204cd1d
--- /dev/null
+++ b/server/src/main/kotlin/eu/kanade/tachiyomi/network/interceptor/NoZstdInterceptor.kt
@@ -0,0 +1,27 @@
+package eu.kanade.tachiyomi.network.interceptor
+
+import okhttp3.Interceptor
+import okhttp3.Response
+
+/**
+ * Yomi patch: Keiyoushi's KeiSource adds `CompressionInterceptor(Brotli, Gzip, Zstd)`, which advertises
+ * zstd. okhttp-zstd decodes through a JNI library that cannot load on iOS (and has no macOS build in
+ * this jar), so any zstd response crashes the call. Registered as a network interceptor, this runs after
+ * every application interceptor and drops zstd from Accept-Encoding; servers fall back to br/gzip,
+ * which decode in pure Java.
+ */
+class NoZstdInterceptor : Interceptor {
+    override fun intercept(chain: Interceptor.Chain): Response {
+        val request = chain.request()
+        val accept = request.header("Accept-Encoding") ?: return chain.proceed(request)
+        val kept =
+            accept
+                .split(',')
+                .map(String::trim)
+                .filter { it.isNotEmpty() && !it.substringBefore(';').trim().equals("zstd", ignoreCase = true) }
+        if (kept.size == accept.split(',').count { it.isNotBlank() }) return chain.proceed(request)
+        val builder = request.newBuilder()
+        if (kept.isEmpty()) builder.removeHeader("Accept-Encoding") else builder.header("Accept-Encoding", kept.joinToString(", "))
+        return chain.proceed(builder.build())
+    }
+}
diff --git a/server/src/main/kotlin/mextensionserver/impl/MExtensionServerLoader.kt b/server/src/main/kotlin/mextensionserver/impl/MExtensionServerLoader.kt
index c9c373a..98ed5c8 100644
--- a/server/src/main/kotlin/mextensionserver/impl/MExtensionServerLoader.kt
+++ b/server/src/main/kotlin/mextensionserver/impl/MExtensionServerLoader.kt
@@ -24,6 +24,17 @@ object MExtensionServerLoader {
     private val logger = KotlinLogging.logger {}
     private val tempDir = Files.createTempDirectory("mextensionserver").toFile()
 
+    /**
+     * Yomi patch: converted JARs persist across launches under the app directory, keyed by the APK's
+     * SHA-256 (the same key as the in-memory cache). dex2jar is the slow part of a first call and ran on every
+     * launch; on the iPhone's interpreter-only JVM that is seconds per extension. The directory name carries the
+     * server revision so a changed converter never reuses stale output.
+     */
+    private val jarCacheDir: File by lazy {
+        val root = System.getProperty("ts.server.rootDir")?.let(::File) ?: tempDir
+        File(root, "extension-jars/${mextensionserver.BuildConfig.REVISION}").apply { mkdirs() }
+    }
+
     // Extensions retain pagination iterators and other request state. Keep one
     // instance per APK for the server lifetime instead of decoding it per call.
     private val loadedExtensions =
@@ -145,13 +156,23 @@ object MExtensionServerLoader {
                 }
             logger.debug { "Main class for extension is $className" }
 
-            // Convert to JAR
-            val dexFile = File(tempApkFile.absolutePath)
-
-            dex2jar(dexFile, jarFile)
-
-            // Extract assets and resources from APK
-            Extension.extractAssetsFromApk(tempApkFile, jarFile)
+            // Convert to JAR, or reuse the JAR converted on an earlier launch (Yomi patch, see jarCacheDir).
+            // The loaded copy stays a temp file because LoadedExtension.close() deletes it.
+            val cachedJar = File(jarCacheDir, "${sha256(apkData)}.jar")
+            if (cachedJar.isFile) {
+                cachedJar.copyTo(jarFile, overwrite = true)
+            } else {
+                dex2jar(File(tempApkFile.absolutePath), jarFile)
+                // Extract assets and resources from APK
+                Extension.extractAssetsFromApk(tempApkFile, jarFile)
+                try {
+                    val partial = File(jarCacheDir, "${cachedJar.name}.${UUID.randomUUID()}.tmp")
+                    jarFile.copyTo(partial, overwrite = true)
+                    if (!partial.renameTo(cachedJar)) partial.delete()
+                } catch (e: Exception) {
+                    logger.warn(e) { "Could not persist converted JAR ${cachedJar.name}" }
+                }
+            }
 
             // Load extension sources
             val loadedSource = loadExtensionSources(jarFile, className, tempApkFile)
